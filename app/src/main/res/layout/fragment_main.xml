<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <!--    titlebar:centerText="首页"-->
    <!--    titlebar:centerTextColor="@color/text"-->
    <!--    titlebar:centerType="textView"-->
    <!--    titlebar:fillStatusBar="false"-->
    <!--    titlebar:leftBackground="@drawable/bg_button"-->
    <!--    titlebar:leftImageResource="@drawable/ic_menu"-->
    <!--    titlebar:leftType="imageView"-->
    <!--    titlebar:rightBackground="@drawable/bg_button"-->
    <!--    titlebar:rightImageResource="@drawable/ic_search"-->
    <!--    titlebar:rightType="imageView"-->
    <!--    titlebar:showBottomLine="false"-->
    <!--    titlebar:titleBarColor="@color/primary"-->

    <com.wuhenzhizao.titlebar.widget.CommonTitleBar
        android:id="@+id/titlebar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:centerText="首页"
        app:layout_constraintBottom_toBottomOf="@+id/layout_content"
       />

    <FrameLayout
        android:id="@+id/layout_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toTopOf="@+id/tab_bottom_nav"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/titlebar" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/tab_bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/background_white"

        app:layout_constraintTop_toBottomOf="@id/layout_content"
        app:menu="@menu/nav_menu" />
</androidx.constraintlayout.widget.ConstraintLayout>